1. Hibernate4不能使用getHibernateTemp()
2. Spring bean id首字母必须小写
3. antlr.collections.AST.getLine()I异常，struts2自带的antlr-2.7.2.jar跟其他引入的antlr包冲突，删除掉一个就好
4. MYECLIPSE默认库文件调整,window->preference->myeclipse->project capabilities->struts2(....)